# Workaround for an npm 11 + Capacitor transitive-dependency bug that
# raises "Invalid Version:" during install. Forces the older, more
# tolerant resolution algorithm.
legacy-peer-deps=true

# Enforce the `engines.node` field in package.json so installs fail
# fast on unsupported Node versions instead of breaking later in the
# build.
engine-strict=true
